export const deposit = (param1: number, param2: number): number=>{
    const addition = param1 + param2;
    return addition
}

export const multiply = (num1: number, num2:number): number=>{
    return num1 * num2;
}